// Empty stub — satisfies pdfjs-dist's optional require('canvas')
// which is only needed for server-side Node.js rendering.
// We use pdfjs-dist in the browser only, so this is never called.
module.exports = {}
